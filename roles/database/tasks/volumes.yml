---
- name: ensure database volume is formatted
  filesystem:
    fstype: ext4
    dev: /dev/vdb

- name: automount volume
  mount:
    boot: yes
    state: mounted
    fstype: ext4
    src: /dev/vdb
    name: /var/lib/postgresql